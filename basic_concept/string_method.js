// // Prompt thr user to enter the full name. Generate a username for them based on the input. Start username with @, followes by ther fullname and ending with thr fullname length


// let fullName = prompt("Enter your fullname without space");
// let userName = "@" + fullName + fullName.length;
// console.log(userName);